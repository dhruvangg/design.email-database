# Usage

- `npm install -g design.email-database`
- add `.env` file

### functions

- `getEmails()` - get all emails with pagination
- `getEmail(id)` - get email by id
- `deleteEmail(id)` - delete email by id
- `createEmail(email)` - create email
- `updateEmail(id, email)` - update email by id

